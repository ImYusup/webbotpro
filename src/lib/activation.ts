// src/lib/activation.ts
export async function activateWhatsAppBot(email: string, productHandle: string) {
  console.log(`✅ Activating WhatsApp bot for ${email} with product ${productHandle}`);

  // Simulasi trigger bot (ganti dengan real logic lo)
  // Misalnya kirim request ke forwarder, atau update DB
  return true;
}
